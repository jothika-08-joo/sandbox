# Expense Tracker (Python)

A simple Python expense tracker application that lets a user record expenses, categorize them, and get a summary.

This project is part of my learning journey in Python and file handling.

---

## 🧠 Features

✔ Record an expense with:
- Expense name  
- Category  
- Amount

✔ Stores the data in `expenses.csv`  
✔ Summarizes total spendings by category  
✔ Calculates remaining budget  

---

## 🗂️ Files in the Project

- `expense.py` — Helper module for the expense data model  
- `expense_tracker.py` — Main program to run the expense tracker  
- `expenses.csv` — Data file that stores expense records  
- `__pycache__/` — Python cache folder (auto-generated)

---

## 🛠️ How to Run

1. Make sure you have Python installed (recommended version: **Python 3.8+**)  
2. Clone the repository:

   ```bash
   git clone https://github.com/jothika-08-joo/sandbox.git